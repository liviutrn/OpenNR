# Research and historical source index

- `native-studies-20260912`: recovered top-level C:/OpenNR study scripts, preserved with dated identity. Run with canonical `tools` on PYTHONPATH and explicit external outputs.
- `legacy/skyrim-workspace`: original packaging/tools/tests/documentation and reports from the unversioned Skyrim parent. Paths and deployment behavior are historical; use the maintained build wrapper for 2.15.0.
- `legacy/video-reference`: relevant video harness source retained for architectural comparison, not the OpenNR runtime authority.
- Dated reports under `docs` and runtime `docs/releases` retain their original version claims and outcomes.
- `refs/archive/legacy/*` contains reconciled runtime histories. The learned-residual student branch remains separate and unpromoted.

Models, weights, caches, captures and generated galleries remain in registered external storage. An archived script is not certified to run against today's datasets or APIs. Preserve its documented provenance and distinguish missing inputs from failed quality gates. No historical model is promoted by the 2.15.0 runtime consolidation.
